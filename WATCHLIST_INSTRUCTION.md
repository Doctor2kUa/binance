# WATCHLIST ANALYSIS INSTRUCTION для AI агента

═══════════════════════════════════════════════════════════════
ОБЩАЯ ЛОГИКА
═══════════════════════════════════════════════════════════════

Агент получает задачу: проанализировать монеты из вотчлиста и дать рекомендацию — открывать позицию или пропустить. Каждая монета оценивается по шкале A-F. Рейтинг ниже B (65%) = пропустить.

═══════════════════════════════════════════════════════════════
ШАГ 1: ПОЛУЧЕНИЕ ДАННЫХ
═══════════════════════════════════════════════════════════════

Для каждой монеты из вотчлиста выполнить:

1. Текущая цена и 24ч изменение:
   GET https://fapi.binance.com/fapi/v1/ticker/24hr?symbol={SYMBOL}USDT

2. Свечные данные (100 дневных свечей):
   GET https://fapi.binance.com/fapi/v1/klines?symbol={SYMBOL}USDT&interval=1d&limit=100

3. Из свечей извлечь:
   - close[] — цены закрытия
   - high[] — максимумы
   - low[] — минимумы
   - volume[] — объёмы
   - Для ATR нужны close[i-1] (предыдущее закрытие)

═══════════════════════════════════════════════════════════════
ШАГ 2: РАСЧЁТ ИНДИКАТОРОВ
═══════════════════════════════════════════════════════════════

Всё считаем самостоятельно через публичный Binance API.

2A. SMA (Simple Moving Average)
─────────────────────────────────
SMA(close[], period) = sum(close[-period:]) / period

Рассчитать:
- SMA7  = SMA(close, 7)
- SMA20 = SMA(close, 20)
- SMA50 = SMA(close, 50)

2B. EMA (Exponential Moving Average)
─────────────────────────────────────
multiplier = 2 / (period + 1)
EMA[0] = SMA(close[:period])
EMA[i] = close[i] * multiplier + EMA[i-1] * (1 - multiplier)

Рассчитать:
- EMA12 = EMA(close, 12)
- EMA26 = EMA(close, 26)

2C. MACD
────────
MACD line = EMA12 - EMA26 (поэлементно, начиная с индекса где обе определены)
Signal line = EMA(MACD line, 9)
MACD histogram = MACD line[-1] - Signal line[-1]

Для оценки входа используем значение MACD histogram (последнее).

2D. RSI (Relative Strength Index, period=14)
─────────────────────────────────────────────
price_changes[i] = close[i] - close[i-1]  (для i = 1..N-1)
gains[i] = max(price_changes[i], 0)
losses[i] = max(-price_changes[i], 0)

avg_gain = sum(gains[:14]) / 14
avg_loss = sum(losses[:14]) / 14

Для i = 14..N-1:
  avg_gain = (avg_gain * 13 + gains[i]) / 14
  avg_loss = (avg_loss * 13 + losses[i]) / 14

RS = avg_gain / max(avg_loss, 1e-10)
RSI = 100 - 100 / (1 + RS)

Используем последнее значение RSI.

2E. Bollinger Bands (period=20, std=2)
──────────────────────────────────────
window = close[-20:]
middle = sum(window) / 20
std = sqrt(sum((x - middle)^2 for x in window) / 20)
lower = middle - 2 * std
upper = middle + 2 * std

BB position = (price - lower) / (upper - lower) * 100

2F. ATR (Average True Range, period=14)
──────────────────────────────────────
Для каждой свечи i (начиная с 1):
  TR[i] = max(
    high[i] - low[i],
    abs(high[i] - close[i-1]),
    abs(low[i] - close[i-1])
  )
ATR = sum(TR[-14:]) / 14

2G. Momentum
───────────
MOM_7d  = (close[-1] / close[-8] - 1) * 100
MOM_30d = (close[-1] / close[-31] - 1) * 100

2H. Volume
─────────
avg_vol_20 = sum(volume[-20:]) / 20
vol_ratio = volume[-1] / avg_vol_20

2I. ATH/ATL
──────────
ATH = max(high)
ATL = min(low)
drop_from_ATH = (price / ATH - 1) * 100

2J. Корреляция с BTC (опционально, но желательно)
─────────────────────────────────────────────────
Получить BTCUSDT свечи за тот же период.
Рассчитать корреляцию Пирсона между close[] монеты и close[] BTC.
Последние 30 свечей.

mean_x = sum(closes_coin[-30:]) / 30
mean_y = sum(closes_btc[-30:]) / 30
cov = sum((closes_coin[i]-mean_x) * (closes_btc[i]-mean_y) for i in -30..-1)
std_x = sqrt(sum((closes_coin[i]-mean_x)^2 for i in -30..-1))
std_y = sqrt(sum((closes_btc[i]-mean_y)^2 for i in -30..-1))
correlation = cov / (std_x * std_y)

═══════════════════════════════════════════════════════════════
ШАГ 3: ОПРЕДЕЛЕНИЕ НАПРАВЛЕНИЯ
═══════════════════════════════════════════════════════════════

Определяем направление (LONG/SKIP) на основе совокупности сигналов.

3A. Подсчёт бычьих/медвежьих сигналов:
──────────────────────────────────────

Бычий сигнал (+1):
- RSI < 35 (перепроданность → лонг выгоден)
- BB position < 30% (у нижней границы → лонг)
- MACD histogram > 0 (бычий моментум)
- Price > SMA50 (долгосрочный тренд бычий)
- Momentum 7d падает > 5% (отскок вероятен)
- Volume ratio > 1.2 (подтверждение интересом)

Медвежий сигнал (-1):
- RSI > 65 (перекупленность)
- BB position > 70% (у верхней границы)
- MACD histogram < 0 (медвежий моментум)
- Price < SMA50 (долгосрочный тренд медвежий)
- Momentum 7d растёт > 10 (коррекция вероятна)
- Volume ratio < 0.5 (слабый интерес)

Нейтральный (0): значения между порогами.

3B. Итоговое направление:
─────────────────────────
Суммируем все сигналы. Максимум +6, минимум -6.
- Сумма >= +3 → рекомендация LONG
- Сумма от -2 до +2 → SKIP (нет ясного направления)
- Сумма <= -2 → SKIP (медвежий сигнал — не торгуем шорты)

═══════════════════════════════════════════════════════════════
ШАГ 4: РАСЧЁТ ENTRY QUALITY SCORE
═══════════════════════════════════════════════════════════════

Максимум 100 баллов. 6 категорий.

Категория 1: RSI (макс 20 баллов)
──────────────────────────────────
- RSI < 20:        20 баллов (глубокая перепроданность)
- RSI 20-30:       18 баллов (перепроданность)
- RSI 30-40:       14 баллов (умеренная перепроданность)
- RSI 40-45:       10 баллов (слабая)
- RSI 45-55:        5 баллов (нейтральная — НЕ зона покупки)
- RSI > 55:         0 баллов (никакой зоны покупки)

Категория 2: Bollinger Bands (макс 20 баллов)
──────────────────────────────────────────────
- BB < 10%:        20 баллов (ниже нижней полосы — экстрем)
- BB 10-20%:       18 баллов (близко к низу)
- BB 20-30%:       14 баллов (нижняя четверть)
- BB 30-40%:        8 баллов (ниже середины)
- BB 40-50%:        4 балла
- BB > 50%:         0 баллов (выше середины — не зона покупки)

Категория 3: MACD Histogram (макс 20 баллов)
─────────────────────────────────────────────
Зависит от масштаба цены. Нормализуем: MACD_norm = MACD_hist / price * 100

Для монет > $10:
- MACD_norm > 3.0:     20 баллов (очень сильный бычий)
- MACD_norm 2.0-3.0:  16 баллов
- MACD_norm 1.0-2.0:  12 баллов
- MACD_norm 0.5-1.0:   8 баллов
- MACD_norm 0.0-0.5:   4 балла
- MACD_norm < 0.0:     0 баллов (медвежий)

Для монет $1-$10 (пороги увеличены в 2x):
- MACD_norm > 6.0:     20 баллов
- MACD_norm 4.0-6.0:  16 баллов
- MACD_norm 2.0-4.0:  12 баллов
- MACD_norm 1.0-2.0:   8 баллов
- MACD_norm 0.0-1.0:   4 балла
- MACD_norm < 0.0:     0 баллов

Для монет < $1 (пороги увеличены в 5x):
- MACD_norm > 15.0:    20 баллов
- MACD_norm 10.0-15.0: 16 баллов
- MACD_norm 5.0-10.0:  12 баллов
- MACD_norm 2.0-5.0:    8 баллов
- MACD_norm 0.0-2.0:    4 балла
- MACD_norm < 0.0:      0 баллов

Категория 4: Volume (макс 15 баллов)
────────────────────────────────────
- vol_ratio > 2.0:  15 баллов (очень высокий интерес)
- vol_ratio 1.5-2.0: 13 баллов
- vol_ratio 1.2-1.5: 10 баллов (выше среднего)
- vol_ratio 0.8-1.2:  7 баллов (нормальный)
- vol_ratio 0.5-0.8:  3 балла
- vol_ratio < 0.5:    0 баллов (мертвый рынок)

Категория 5: SMA Trend (макс 15 баллов)
───────────────────────────────────────
Считаем сколько SMA цена превышает:
- Все 3 (SMA7, SMA20, SMA50):  15 баллов (сильный бычий тренд)
- 2 из 3:                       11 баллов
- 1 из 3:                        5 баллов (слабый тренд)
- 0 из 3:                        0 баллов (все выше цены — медвежий)

Бонус: если Price > SMA50 — +2 бонусных балла (макс всё равно 15).

Категория 6: Momentum + Mean Reversion (макс 10 баллов)
───────────────────────────────────────────────────────
Логика: после сильного падения отскок вероятен.
- MOM_7d < -20%:    10 баллов (обвал — высокий отскок)
- MOM_7d -15% to -20%: 8 баллов
- MOM_7d -10% to -15%: 6 баллов
- MOM_7d -5% to -10%:  3 балла
- MOM_7d 0% to -5%:    1 балл
- MOM_7d > 0%:         0 баллов (рост продолжается — не отскок)

Пенальти за моментум > +15% (уже уехало, не гонимся):
- MOM_7d > +15%: штраф -5 к итоговому скору

═══════════════════════════════════════════════════════════════
ШАГ 5: ШТРАФЫ И ПЕНАЛТИ
═══════════════════════════════════════════════════════════════
(Вычитаются из итогового скора)

Штраф 1: Memecoin Penalty
─────────────────────────
Если монета — мемкоин (DOGE, SHIB, PEPE, FLOKI, BONK, WIF, SATS и т.д.):
- Штраф -20 баллов

Логика: мемкоины непредсказуемы, слишком зависимы от хайпа.

Штраф 2: Low Liquidity
──────────────────────
Если avg_vol_20 < $100,000 (дневной объём в USDT):
- Штраф -15 баллов

Штраф 3: Extreme ATR
───────────────────
Если ATR/price > 15% (суточная волатильность):
- Штраф -10 баллов

Штраф 4: Already Run
───────────────────
Если MOM_7d > +25% за неделю:
- Штраф -15 баллов (уже уехало, не гонимся)

Штраф 5: Dead Zone
──────────────────
Если RSI 40-55 И BB 40-60% И MACD около 0:
- Штраф -10 баллов (нет ясного сигнала)

═══════════════════════════════════════════════════════════════
ШАГ 6: ИТОГОВЫЙ РЕЙТИНГ
═══════════════════════════════════════════════════════════════

Итоговый скор = сумма категорий (макс 100) — штрафы
Итоговый скор ограничен: минимум 0, макс 100.

Перевод в буквенный рейтинг:
- A: 80-100%   (Отличный вход)
- B: 65-79%    (Хороший вход)
- C: 50-64%    (Средний вход)
- D: 35-49%    (Слабый вход)
- F: 0-34%     (Плохой вход)

ПРАВИЛО: рейтинг ниже B = ПРОПУСКАЕМ. Только A или B → рекомендуем вход.

═══════════════════════════════════════════════════════════════
ШАГ 7: ОПРЕДЕЛЕНИЕ ПАРАМЕТРОВ ВХОДА
═══════════════════════════════════════════════════════════════

Если рейтинг A или B → рассчитываем параметры.

7A. Risk Per Trade
──────────────────
Фиксированный риск на сделку: 2% от депозита.
risk_per_trade = deposit * 0.02

7B. Stop Loss
─────────────
SL = price - ATR * 1.5 (для лонга)

Минимальный SL: не менее 3% от цены.
Максимальный SL: не более 15% от цены.

Если SL < 3%: SL = price * 0.97
Если SL > 15%: SL = price * 0.85

7C. Position Size
─────────────────
risk_amount = deposit * 0.02
sl_distance = price - SL
position_value = risk_amount / (sl_distance / price)

Проверки:
- position_value >= 10 USDT (минимум на Binance)
- position_value <= deposit * 0.5 (не более 50% депозита на одну сделку)

7D. Take Profits
────────────────
TP1 = price + (price - SL) * 1.5  (R:R 1.5:1)
TP2 = price + (price - SL) * 2.5  (R:R 2.5:1)
TP3 = price + (price - SL) * 4.0  (R:R 4:1)

7E. Leverage
───────────
Рассчитываем из position_value и amount:
leverage = round(position_value / amount)

Ограничения:
- Минимум: 1x
- Максимум: 5x (для безопасности)
- Если leverage > 5: leverage = 5, пересчитать position_value = amount * 5
- Если leverage < 1: leverage = 1

7F. Partial Take Profit Plan
────────────────────────────
- TP1: закрыть 30% позиции
- TP2: закрыть 50% от оставшегося (35% от начальной)
- TP3: закрыть 100% остатка (35% от начальной)
- Trail SL после TP1: подтянуть SL к entry price

═══════════════════════════════════════════════════════════════
ШАГ 8: AI RISK ASSESSMENT
═══════════════════════════════════════════════════════════════

Отдельная оценка риска AI (0-100) — это НЕ тот же скор что entry quality.
Это оценка "опасности" позиции.

Danger Score:
─────────────
+25 если RSI > 60 (перекупленность — лонг опасен)
+20 если RSI < 20 (глубокая перепроданность — может падать дальше)
+20 если BB < 10% (ниже BB — экстремальная зона)
+15 если MACD < 0 (медвежий моментум)
+10 если Price < SMA20 (краткосрочный медвежий тренд)
+10 если Price < SMA50 (долгосрочный медвежий тренд)
+10 если MOM_7d > +20% (перегрет — коррекция)
+15 если MOM_7d < -20% (обвал — может продолжиться)
+5  если Vol ratio < 0.5 (низкая ликвидность)
+5  если MCAP rank > 200 (мелкая монета)

Danger Score перевод в уровни:
- 0-29:   🟢 LOW
- 30-59:  🟡 MEDIUM
- 60-100: 🔴 HIGH

═══════════════════════════════════════════════════════════════
ШАГ 9: ВЕРДИКТ
═══════════════════════════════════════════════════════════════

Формируем вердикт по шаблону:

ENTRY VERDICT
═════════════
Монета: {NAME}/USDT
Цена: ${PRICE}
Направление: LONG / SKIP

Entry Quality: {SCORE}/100 ({RATING})
Risk Level: {LOW/MEDIUM/HIGH}

Если SKIP (рейтинг ниже B):
  Причина: {основные причины}
  Рекомендация: Пропустить. / Подождать отката к ${LEVEL}.

Если A или B (рекомендуем вход):
  Entry: ${ENTRY_PRICE}
  SL: ${SL_PRICE} ({SL_PCT}%)
  TP1: ${TP1} ({TP1_PCT}%) — закрыть 30%
  TP2: ${TP2} ({TP2_PCT}%) — закрыть 35%
  TP3: ${TP3} ({TP3_PCT}%) — закрыть 35%
  Size: ${POSITION_VALUE} | Lev: ${LEV}x
  R:R (TP1): 1.5:1 | R:R (TP3): 4:1

  После TP1: trail SL → entry
  Стратегия: Partial TP 30/35/35, trail SL

  Key Risks:
  - {риск 1}
  - {риск 2}
  - {риск 3}

═══════════════════════════════════════════════════════════════
ПРИМЕР РАБОТЫ
═══════════════════════════════════════════════════════════════

Допустим, монета EDEN/USDT, цена $0.092:
- RSI 31 → 18 баллов (перепроданность)
- BB 18% → 18 баллов (близко к низу)
- MACD +0.0015, norm = 0.16% → 8 баллов (слабый)
- Vol 0.4x → 0 баллов (низкий)
- SMA: 0/3 above → 0 баллов
- MOM_7d -22% → 10 баллов (отскок)
- No memecoin → нет штрафа
- No dead zone → нет штрафа

Raw: 18+18+8+0+0+10 = 54
Penalties: -10 (low vol) = 44
Rating: 44% → D → SKIP

Причина: Нет объёма, слабый MACD, против тренда.

═══════════════════════════════════════════════════════════════
ДОПОЛНИТЕЛЬНЫЕ ПРАВИЛА
═══════════════════════════════════════════════════════════════

1. Не более 3 одновременных открытых позиций
2. Если портфель уже несёт 2+ HIGH RISK позиций — не открывать новые
3. Не входить если BTC MOM_7d < -10% (общий рынок падает)
4. Мемкоины: автоматический SKIP независимо от скора
5. Минимальный R:R = 1.5:1 — если TP1 даёт меньше, SKIP
6. Не гнаться за поездом: если MOM_7d > +20% → SKIP
7. SL никогда не дальше 15% (контроль риска)

═══════════════════════════════════════════════════════════════
ИСТОЧНИКИ ДАННЫХ
═══════════════════════════════════════════════════════════════

ВСЕ данные из публичного Binance API:
- https://fapi.binance.com/fapi/v1/ticker/24hr?symbol={SYM}USDT
- https://fapi.binance.com/fapi/v1/klines?symbol={SYM}USDT&interval={INT}&limit={N}

Интервалы свечей:
- Для скальпинга: 1h, 4h
- Для свинга: 1d
- Для BTC корреляции: 1d

API ключи НЕ нужны. Rate limit: 1200 req/min.

═══════════════════════════════════════════════════════════════

Ключевые принципы:
1. Риски > Оптимизма
2. Никаких "может взлететь" без данных
3. SL всегда
4. Partial TP всегда
5. Ниже B = не входим
